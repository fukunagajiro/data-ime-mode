# install.ps1 が生成するネイティブメッセージングホストのマニフェスト。
# 絶対パスと拡張機能 ID を含むマシン固有ファイルなのでコミットしない。
host/*.json

# 動作確認用の一時ファイル
_*

# 診断ログ（host\debug.on を置くと ime-host.ps1 が出力する）
host/debug.on
host/debug.log

# Windows
Thumbs.db
desktop.ini

# 拡張機能 ID の元になる秘密鍵（manifest には公開鍵だけを載せる）
*.pem
